using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Tilemaps;

public class Room : MonoBehaviour
{
    [SerializeField]
    public Tilemap tilemap;

    // Making a single room



    private int height = 1;

    private int width = 1;
    

   // public void BoxFill(Vector3Int position, tilemap.RogueLikeDungeonV1 Roguelike_Tileset_concept_5, int startX, int startY, int width, int height);




















































    // https://forum.unity.com/threads/2d-tile-mapping-in-unity-demystified.441444/

    // Create starting room at (X,Y) on a grid (centre of the grid)

    // Choose a room size

    // Choose a direction a check if the cell in that direction (adjacent to the starting room) is available

    // Do this until the room size is met or it cant do that room. If it cant do that room then it will restart by choosing a room size

    // Once a room reaches the (edge of the grid -1) the boss room will be constructed and connected to it

    // The map will stop being constructed and then checked to see if it meets the requirements of the map

    // if so it will be rendered, if not the process will restart































    // Inputs

    // Min Steps. 

    // Grid Max Radius. 

    // Min Dead Ends.

    // Min Num of Loops.


    // Process 



    // Outputs


    // Map

    // Spacially Logical

    // Room

    // Start

    // End 
}
